#:schema https://docs.jj-vcs.dev/latest/config-schema.json

[user]
name = "Aplmint"
email = "prism2048@outlook.com"

[git]
private-commits = "empty()"
# fetch = "upstream"
# push = "origin"

[ui]
default-command = ["log", "--limit", "7"]
